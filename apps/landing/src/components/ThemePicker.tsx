// ThemePicker simplified - only one theme available, so this component is hidden
// Keeping it for backward compatibility but it renders nothing

export const ThemePicker = () => {
  return null;
};
