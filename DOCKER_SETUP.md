# Docker настройка (опционально)

## Проблема с правами доступа к Docker

Если вы видите ошибку:

```
permission denied while trying to connect to the Docker daemon socket
```

Это означает, что ваш пользователь не в группе `docker`. База данных может быть запущена из другого проекта (workyy-dev) или через sudo.

## Варианты решения:

### Вариант 1: Добавить пользователя в группу docker (рекомендуется)

```bash
sudo usermod -aG docker $USER
# Выйдите и войдите снова, чтобы изменения вступили в силу
# Или выполните:
newgrp docker
```

После этого проверьте:

```bash
docker ps
# Должно работать без sudo
```

### Вариант 2: Использовать sudo (не рекомендуется для обычной работы)

```bash
sudo docker compose up -d postgres
```

### Вариант 3: Использовать уже запущенную базу (если она работает)

Если PostgreSQL уже запущен на порту 5433 (например, из workyy-dev),
можно просто использовать её. Проект будет подключаться к ней автоматически.

Проверка:

```bash
netstat -tlnp | grep :5433
# Или
ss -tlnp | grep :5433
```

## Исправление предупреждения о version

Предупреждение о `version: "3.9"` уже исправлено - этот атрибут устарел в новых версиях Docker Compose.

## Текущий статус

✅ База данных запущена и доступна на порту 5433  
✅ Проект работает через `pnpm dev`  
⚠️ Если хотите использовать Docker Compose для управления БД - настройте права доступа
